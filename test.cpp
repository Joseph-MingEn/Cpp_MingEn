#include <bits/stdc++.h>
using namespace std;

int main() {
    // ios::sync_with_stdio(0);
    // cin.tie(0);
    long long int i, j;

    while(cin >> i >> j) {
        cout << abs(i - j);

    }

    return 0;

}